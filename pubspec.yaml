name: swipe_gesture_recognizer
description: A simple and lightweight swipe gesture recognizer for a widget in Flutter.

homepage: https://github.com/BilalShahid13/FlutterSwipeGestureRecognizer
repository: https://github.com/BilalShahid13/FlutterSwipeGestureRecognizer
version: 0.0.2

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
